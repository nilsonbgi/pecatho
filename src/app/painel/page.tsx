import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server";
export default async function Painel(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:profile}=await supabase.from("profiles").select("display_name,username,status").eq("id",user.id).single();return <main className="shell"><nav className="topbar"><div className="brand"><span className="brandMark">P</span><span>Pecatho</span></div></nav><section className="hero"><div className="eyebrow">ÁREA AUTENTICADA</div><h1>Olá, <em>{profile?.display_name||user.email}</em></h1><p className="heroCopy">Sua identidade Pecatho está ativa. O próximo passo será o gerenciamento completo do perfil, anúncio, mídia, publicação e plano.</p><section className="pillars"><article className="card"><h2>Conta</h2><p>Status: {profile?.status||"pending"}</p></article><article className="card"><h2>Perfil</h2><p>{profile?.username ? "@"+profile.username : "Ainda não configurado"}</p></article><article className="card"><h2>Anúncio</h2><p>Pronto para configuração.</p></article><article className="card"><h2>Segurança</h2><p>Supabase Auth + RLS.</p></article></section></section></main>
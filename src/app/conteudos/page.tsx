import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function ConteudosPage(){
 const supabase=await createClient();
 const {data:products}=await supabase.from("digital_content_products").select("id,title,description,product_type,price,currency,owner_type").eq("status","published").order("created_at",{ascending:false}).limit(60);
 return <main className="min-h-screen bg-[#f5f5f7] px-4 py-8 text-slate-950"><section className="mx-auto max-w-7xl"><div className="rounded-[30px] bg-[#090a0f] p-7 text-white sm:p-10"><div className="text-[10px] font-black tracking-[.2em] text-violet-300">PECATHO · LOJA</div><h1 className="mt-2 text-4xl font-black tracking-[-.06em]">Conteúdo exclusivo</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">Imagens, vídeos e pacotes publicados por anunciantes e criadores. Escolha, pague e receba o acesso aos arquivos após a confirmação.</p></div><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{(products??[]).map(p=><Link key={p.id} href={`/conteudos/${p.id}`} className="rounded-3xl border border-slate-200 bg-white p-5 no-underline shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div className="text-[10px] font-black tracking-[.14em] text-violet-600">{p.product_type==="package"?"PACOTE":p.product_type==="single_video"?"VÍDEO":"IMAGEM"}</div><h2 className="mt-2 text-lg font-black text-slate-950">{p.title}</h2><p className="mt-2 line-clamp-3 text-xs leading-5 text-slate-500">{p.description||"Conteúdo digital exclusivo."}</p><div className="mt-5 flex items-center justify-between"><strong className="text-lg">R$ {Number(p.price).toFixed(2).replace(".",",")}</strong><span className="rounded-xl bg-slate-950 px-3 py-2 text-xs font-black text-white">Ver conteúdo</span></div></Link>)}</div>{(products??[]).length===0&&<div className="mt-6 rounded-3xl border border-dashed border-slate-300 bg-white p-10 text-center text-sm text-slate-500">Ainda não há conteúdos publicados para venda.</div>}</section></main>;
}